# Flask — lightweight WSGI framework for simulating a customer origin server.
# Pinned version ensures reproducible builds across interview demo environments.
Flask==3.1.0
Werkzeug==3.1.3

# Gunicorn — production-grade WSGI server for containers.
# The Flask development server is intentionally not used in the image entrypoint.
gunicorn==23.0.0
